import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#260C1A",
    },
    secondary: {
      main: "#432E36",
    },
    spacing: 8,
  },
});

export default theme;
